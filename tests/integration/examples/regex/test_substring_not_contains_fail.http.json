{
    "fixtures": [
        "server"
    ],
    "vars": {
        "api_base": "http://localhost:5000"
    },
    "stages": [
        {
            "name": "test_substring_should_not_be_present",
            "request": {
                "url": "{{ api_base }}/error-response",
                "method": "GET"
            },
            "response": {
                "verify": {
                    "status": 400,
                    "body": {
                        "not_contains": [
                            "error",
                            "Invalid request"
                        ]
                    }
                }
            }
        }
    ]
}