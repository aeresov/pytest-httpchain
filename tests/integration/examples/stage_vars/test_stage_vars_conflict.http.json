{
    "fixtures": ["server"],
    "stages": [
        {
            "name": "test_fixture_var_conflict",
            "fixtures": ["server"],
            "vars": {
                "server": "this_should_conflict"
            },
            "request": {
                "url": "http://localhost:5000/echo/test",
                "method": "GET"
            }
        }
    ]
}