{
    "fixtures": [
        "server"
    ],
    "vars": {
        "api_base": "http://localhost:5000"
    },
    "stages": [
        {
            "name": "no_timeout_test",
            "request": {
                "url": "{{ api_base }}/fast",
                "method": "GET"
            },
            "response": {
                "verify": {
                    "status": 200
                }
            }
        }
    ]
}