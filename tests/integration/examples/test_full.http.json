{
    "fixtures": [
        "server",
        "string_value",
        "number_value",
        "dict_value"
    ],
    "vars": {
        "api_base": "http://localhost:5000"
    },
    "flow": [
        {
            "$ref": "stage_ref.json#/call_ok"
        },
        {
            "name": "path_param_string",
            "request": {
                "url": "{{ api_base }}/path_param_string/{{ string_value }}"
            }
        }
    ],
    "final": [
        {
            "$ref": "stage_ref.json#/call_bad"
        }
    ]
}