{
    "fixtures": ["server"],
    "vars": {
        "api_base": "http://localhost:5000"
    },
    "stages": [
        {
            "name": "save_integer_variable", 
            "request": {
                "url": "{{ api_base }}/get_number",
                "method": "GET"
            },
            "response": {
                "verify": {
                    "status": 200
                },
                "save": {
                    "vars": {
                        "saved_id": "id"
                    }
                }
            }
        },
        {
            "name": "verify_integer_variable",
            "request": {
                "url": "{{ api_base }}/get_another_number", 
                "method": "GET"
            },
            "response": {
                "save": {
                    "vars": {
                        "response_result": "result"
                    }
                },
                "verify": {
                    "status": 200,
                    "vars": {
                        "response_result": "{{ saved_id }}"
                    }
                }
            }
        }
    ]
}