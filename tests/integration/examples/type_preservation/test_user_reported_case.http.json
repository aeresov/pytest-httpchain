{
    "fixtures": ["server"],
    "vars": {
        "api_base": "http://localhost:5000"
    },
    "stages": [
        {
            "name": "Check playlist #1 added",
            "request": {
                "url": "{{ api_base }}/get_number",
                "method": "GET"
            },
            "response": {
                "verify": {
                    "status": 200
                },
                "save": {
                    "vars": {
                        "result": "id"
                    }
                }
            }
        },
        {
            "name": "Verify playlist matches",
            "request": {
                "url": "{{ api_base }}/get_another_number",
                "method": "GET"
            },
            "response": {
                "verify": {
                    "status": 200,
                    "vars": {
                        "result": "{{ result }}"
                    }
                }
            }
        }
    ]
}