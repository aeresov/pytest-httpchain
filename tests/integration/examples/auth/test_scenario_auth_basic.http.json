{
    "fixtures": ["server"],
    "auth": "auth_functions:basic_auth",
    "vars": {
        "api_base": "http://localhost:5000"
    },
    "stages": [
        {
            "name": "test_public_endpoint",
            "request": {
                "url": "{{ api_base }}/api/public",
                "method": "GET"
            },
            "response": {
                "verify": {
                    "status": 200
                }
            }
        },
        {
            "name": "test_protected_endpoint",
            "request": {
                "url": "{{ api_base }}/api/protected",
                "method": "GET"
            },
            "response": {
                "verify": {
                    "status": 200,
                    "body": {
                        "contains": ["protected data", "user"]
                    }
                }
            }
        }
    ]
}