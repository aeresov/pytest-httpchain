{
    "fixtures": [
        "server"
    ],
    "vars": {
        "api_base": "http://localhost:5000"
    },
    "stages": [
        {
            "name": "test_missing_header_verification",
            "request": {
                "url": "{{ api_base }}/headers",
                "method": "GET"
            },
            "response": {
                "verify": {
                    "status": 200,
                    "headers": {
                        "X-Non-Existent-Header": "some-value"
                    }
                }
            }
        }
    ]
}