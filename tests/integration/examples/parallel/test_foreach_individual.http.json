{
    "stages": [
        {
            "name": "foreach_users",
            "description": "Fetch multiple users using foreach with individual parameter",
            "fixtures": ["server"],
            "parallel": {
                "foreach": [
                    {
                        "individual": {
                            "user_id": [1, 2, 3]
                        }
                    }
                ]
            },
            "request": {
                "url": "{{ server }}/user/{{ user_id }}"
            },
            "response": [
                {
                    "verify": {
                        "status": 200
                    }
                },
                {
                    "save": {
                        "jmespath": {
                            "response_id": "id"
                        }
                    }
                },
                {
                    "verify": {
                        "expressions": [
                            "{{ response_id == user_id }}"
                        ]
                    }
                }
            ]
        }
    ]
}
