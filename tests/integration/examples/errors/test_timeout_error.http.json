{
    "stages": [
        {
            "name": "timeout_error",
            "description": "Request should timeout",
            "fixtures": ["server"],
            "request": {
                "url": "{{ server }}/delay_ms/600",
                "timeout": 0.1
            },
            "response": [
                {
                    "verify": {
                        "status": 200
                    }
                }
            ]
        }
    ]
}
