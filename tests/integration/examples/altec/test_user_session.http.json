{
    "stages": [
        {
            "fixtures": ["settings"],
            "vars": {
                "api_base": "https://{{ settings.host }}:{{ settings.port }}",
                "username": "{{ settings.username }}",
                "password": "{{ settings.password }}"
            },
            "$ref": "session.json#/login"
        },
        {
            "fixtures": ["settings"],
            "vars": {
                "api_base": "https://{{ settings.host }}:{{ settings.port }}"
            },
            "$ref": "session.json#/logout"
        }
    ]
}