[project]
name = "sandbox"
version = "0.1.0"
description = "Sandbox."
readme = "README.md"
requires-python = ">=3.13"
authors = [
    { name = "Alexander Eresov", email = "Alexander.Eresov@soundunited.com" },
]
dependencies = [
    "pydantic>=2.11.7",
    "pytest>=8.0.0",
    "jsonref>=1.1.0",
    "jinja2>=3.1.6",
]

[dependency-groups]
dev = [
    "ruff>=0.12.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.entry-points.pytest11]
sandbox = "sandbox.pytest_plugin"

[tool.hatch.build]
packages = ["sandbox"]

[tool.ruff]
line-length = 180
target-version = "py313"

[tool.ruff.format]
quote-style = "double"
line-ending = "lf"

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["tests", "utils"]

[tool.pytest.ini_options]
log_cli = true
log_cli_level = "INFO"
markers = [
    "test: generic marker",
]

[tool.pyright]
exclude = ["cdk.out/"]
ignore = [".venv/"]
reportMissingImports = "error"
reportMissingTypeStubs = false
reportExplicitAny = false
typeCheckingMode = "off"
pythonPlatform = "Linux"
